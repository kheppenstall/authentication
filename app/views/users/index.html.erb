<h2>All Users</h2>

<ul class="list-group">
  <% @users.each do |user| %>
    <li class="list-group-item"><%= link_to "User #{user.id}: #{user.name}, #{user.email}", user_path(user) %></li>
  <% end %>
</ul>